            var randomNumber = Math.floor(Math.random() * quotes.length); // 랜덤으로 이미지와 대사를 선택하기 위한 인덱스 생성
            document.getElementById("image").src = quotes[randomNumber].imagePath; // 이미지 변경
            document.getElementById("text").innerHTML = quotes[randomNumber].text; // 대사 변경
ㅡㅡㅡㅡㅡㅡㅡㅡ

